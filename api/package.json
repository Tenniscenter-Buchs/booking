{
    "name": "booking-api",
    "version": "1.0.0",
    "description": "",
    "scripts": {
        "test": "echo \"no tests to run\" && exit 0",
        "build": "tsc",
        "start": "node build/index.js",
        "start:dev": "nodemon build/index.js",
        "dev": "export DATABASE_URL=$(heroku config -a booking-staging-api | grep DATABASE_URL | awk \"{ print $2 }\") && npm run start:dev"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "express": "^4.17.3"
    },
    "devDependencies": {
        "@types/express": "^4.17.13",
        "@types/node": "^17.0.21",
        "nodemon": "^2.0.15",
        "ts-node": "^10.7.0",
        "typescript": "^4.6.2"
    }
}
